
android {
    buildToolsVersion '30.0.3'

    compileSdkVersion 30

    ndkVersion "22.0.7026061"

    defaultConfig {
        minSdkVersion 21
        targetSdkVersion 30
        versionCode = 248
        versionName = "3.0.248 (beta)"
    }
}
