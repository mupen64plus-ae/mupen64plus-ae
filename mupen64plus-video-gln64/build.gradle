apply from: '../native_common.gradle'

android {

    packagingOptions {
        exclude 'lib/x86/libc++_shared.so'
        exclude 'lib/armeabi-v7a/libc++_shared.so'
        exclude 'lib/x86/libSDL2.so'
        exclude 'lib/armeabi-v7a/libSDL2.so'
    }
}
